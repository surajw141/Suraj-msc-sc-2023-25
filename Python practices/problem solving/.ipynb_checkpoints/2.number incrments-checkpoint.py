#Create a function (def) that takes a number as an argument, increments the number by +1 and returns the result.
def increment_number(num):
    """
    Increments a given number by 1.

    Args:
        num (int or float): The number to be incremented.

    Returns:
        int or float: The incremented number.
    """
    return num + 1
    print(num)